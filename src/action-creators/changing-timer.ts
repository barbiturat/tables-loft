import {CHANGING_TIMER} from '../constants/action-names';
import {createSimpleAction} from '../helpers/index';

const changingTimer = createSimpleAction(CHANGING_TIMER);

export default changingTimer;
